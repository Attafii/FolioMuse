/**
 * FolioMuse homepage (Section 02 · Experience design).
 * Gallery-first entry point — built out by T7+ of .sisyphus/plans/homepage.md.
 * This is a temporary placeholder; the real homepage sections land in Wave 1/2.
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex w-full max-w-6xl flex-col gap-8 px-4 py-12 sm:px-6 lg:px-8">
      <p className="font-mono text-sm text-muted-foreground">FolioMuse</p>
      <h1 className="font-display text-3xl font-semibold tracking-tighter sm:text-4xl">
        Homepage coming soon
      </h1>
      <p className="max-w-prose text-muted-foreground">
        The gallery-first entry point is under construction.
      </p>
    </main>
  );
}

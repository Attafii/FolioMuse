# FolioMuse â€” Product Decision Log

This file is **append-only**. Never edit or delete a prior entry â€” if a decision is reversed, add a new entry that supersedes it and references the entry number it supersedes.

Format per entry:

```
## D-<number> â€” <short title>
Date: <date>
Status: Accepted | Superseded by D-<n>
Context: <why this decision was needed>
Decision: <what was decided>
Consequences: <what this enables/constrains going forward>
Related: <ADR-xxx, charter section, etc.>
```

---

## D-1 â€” Establish product charter as the canonical source of truth

Date: 2026
Status: Accepted
Context: FolioMuse is a greenfield repository (only `LICENSE` existed prior to this work). Section 01 Â· Product foundation requires a charter, personas, JTBD, non-goals, success metrics, content-quality principles, originality rules, and this decision log before any feature code is written.
Decision: Created `docs/product/charter.md` as the canonical, binding definition of product boundary, differentiated promise, and success criteria, with supporting documents (`personas.md`, `jobs-to-be-done.md`, `success-metrics.md`, `content-quality-principles.md`, `originality-rules.md`) and this decision log. Also created `AGENTS.md` at repo root to encode agent operating rules (skill gates, stack conventions, architectural boundaries, quality gates) referencing the charter.
Consequences: All future features (human gallery, section intelligence, MCP agent) must be checked against the non-goals (especially the anti-cloning rules, NG1/NG3/NG4) and the north-star metric before implementation. Any conflict must be raised, not silently overridden.
Related: `docs/product/charter.md`, `AGENTS.md`, `docs/product/originality-rules.md`

## D-2 â€” North-star metric defined as "weekly published portfolio rate" (original content only)

Date: 2026
Status: Accepted
Context: Needed a single north-star metric that captures value across all three pillars (gallery, section intelligence, MCP agent) without rewarding cloning behavior.
Decision: North-star metric is "published portfolios per active builder, per week," explicitly excluding/flagging near-duplicate publishes via the originality-score guardrail metric.
Consequences: Any feature that increases raw engagement (e.g., gallery page views) without contributing to original, published output is not credited toward the north-star. Analytics/telemetry work in future features must be able to compute the originality-score guardrail.
Related: `docs/product/success-metrics.md`

## D-3 â€” Skills environment gap noted

Date: 2026
Status: Accepted (tracked as open risk)
Context: The task required loading a "Taste Skill" before UI work and a "Ponytail Skill" after tests pass. At execution time, `read_skill` reported no skills installed in this environment.
Decision: Documented the gate requirement in `AGENTS.md` Â§3 regardless of current availability, and proceeded with this docs-only feature (which triggers neither gate, since it contains no UI code and no code to simplify). Skill availability must be re-checked before Section 02+ work that touches UI.
Consequences: Any future feature that touches UI must re-verify skill availability and block if the Taste Skill is still unavailable, per `AGENTS.md`.
Related: `AGENTS.md` Â§3

---

## D-4 â€” Editorial acceptance and curation rubric with tiered consent, L0â€“L4 quality levels, and mandatory compliance gate

Date: 2026
Status: Accepted
Context: Section 01 requires a repeatable editorial standard for what enters the gallery corpus. The curation rubric (`docs/product/curation-rubric.md`) and gallery schema (ADR-0002, `docs/adr/0002-gallery-schema-design.md`) define the standard and its persistence shape. This decision locks the rubric's multi-dimensional structure (Quality Ã— Compliance), tiered consent model (`DISPLAY` / `PATTERN_DERIVE` / `FULL`), append-only audit log, and originality-score schema-readiness (fields reserved, algorithm deferred per R8).
Decision: (1) Accept the curation rubric as binding for any feature touching the gallery corpus. (2) Adopt a multi-dimensional rubric â€” quality L0â€“L4 scored independently from a mandatory compliance gate (`PASS` / `FLAG` / `FAIL`). An item with `compliance = FAIL` cannot be accepted regardless of quality score. (3) Adopt a tiered consent model with a `ConsentRecord` table: `DISPLAY` (gallery display only), `PATTERN_DERIVE` (derivation for pattern signals permitted), `FULL` (unrestricted within FolioMuse's anti-cloning guardrails). (4) Adopt an append-only `AuditEntry` model â€” no update or delete in the domain interface; audit integrity is enforced at the contract level. (5) Reserve `structureFingerprint` and `contentHash` fields on `GalleryItem` for future originality-score computation; the algorithm is deferred per R8's open questions.
Consequences: No item may be accepted into the gallery corpus without `compliance = PASS`, quality â‰¥ L2, a valid consent record, and complete attribution. Consent revocation triggers `ARCHIVED` status (never deletion); affected pattern signals are marked stale via `staleSince`. Emergency takedown allows any reviewer to set `SUSPENDED` immediately, with full review required within 48 hours. Reviewer conflicts (>1 level quality score difference) auto-escalate to a senior reviewer; the senior decision is binding and logged as `OVERRIDE`. Stale-content threshold is 18 months since last review, triggering `ARCHIVED`. Open questions deferred to future ADRs: originality-score algorithm and threshold, publish-time enforcement level (warn vs. block), and data-retention policy for gallery attribution records.
Related: `docs/product/curation-rubric.md`, `docs/adr/0002-gallery-schema-design.md`, `docs/product/originality-rules.md` (R1â€“R8), `docs/product/charter.md` (NG1, NG3, NG4)

## D-5 â€” Provenance and originality policy: PROV-O provenance, licence-consent intersection, immutable attribution, durable removal, and derivative rebuild with a strengthened R2 diversity floor

Date: 2026
Status: Accepted
Context: ADR-0001/0002 established the anti-cloning boundary and gallery schema but left provenance-specific questions open: how provenance is modelled, how licence and consent intersect, how creator identity is canonicalized, how AI contributions are disclosed without storing raw content, how ownership claims and removals are adjudicated, and how derivatives are invalidated and rebuilt after removal. Section 01 requires these to be binding before provenance persistence and service code are written.
Decision: (1) Adopt W3C PROV-O entity/activity/agent provenance shape via new `Creator`, `SourceRecord`, `AiProvenance`, `OwnershipClaim`, and `RemovalRecord` models plus `ConsentRecord.revokedAt`, rolled out additively with no backfill of guessed provenance (`UNKNOWN` disclosure where unknown). (2) Effective permission is the derived intersection of licence, consent tier, and policy â€” never a stored mutable permission field; ND licences are display-only for derivation regardless of consent, and NC items are display-only until commercial posture is decided. (3) Attribution is immutable (R3); corrections use superseding provenance assertions, never mutation; creator canonicalization is explicit only (no fuzzy auto-merge). (4) AI provenance is metadata-minimized: hashes plus provider/model/timestamp/disclosure, never raw prompts or outputs. (5) Ownership claims follow PENDING â†’ UNDER_REVIEW â†’ ACCEPTED|REJECTED (credible claims may SUSPEND immediately); removal is durable (`REQUESTED` â†’ `EFFECTIVE` â†’ `COMPLETED`), never a hard delete. (6) Derivative rebuild is async and idempotent: removal marks affected PatternSignals stale and queues rebuild; the R2 floor is strengthened to **â‰¥3 eligible items AND â‰¥2 distinct creators** (diversity floor); signals below floor enter `DROPPED_BELOW_FLOOR` (not deleted). (7) Safe projection boundary: no public/API/MCP contract returns `contentBlob` + `structureJSON` together, exposes raw captures/prompts/claimant evidence, or exposes a full-item fetch tool.
Consequences: Every artifact is attributable, every permission is provable, and every AI contribution is disclosed. Removal satisfies legal/audit durability without enabling copying, and the â‰¥2-creator diversity floor closes a single-creator aggregation loophole. Constraints that follow: no automated creator merging, no hard deletes (row count grows monotonically), no derivation from ND/NC items, rebuild requires the floor (below-floor signals are dropped, not repaired by weakening R2), and the MCP read surface never returns full gallery content. Deferred: commercial-use posture for NC items, data-retention duration, originality-score algorithm/enforcement, crawler/API ingestion modes, verified creator identity (W3C VC). The binding policy is `docs/product/provenance-and-originality-policy.md`; architectural implementation is ADR-0003. Supersedes nothing; D-1â€“D-4 remain in force.
Related: `docs/product/provenance-and-originality-policy.md`, `docs/adr/0003-provenance-and-originality-policy.md`, `docs/adr/0002-gallery-schema-design.md`, `docs/product/originality-rules.md` (R2, R3, R5, R6), `docs/product/charter.md` (NG1, NG3, NG4)

## D-6 â€” Data flywheel and ranking feedback: action-weighted, saturation-limited, diversity-capped behavior-driven discovery

Date: 2026
Status: Accepted
Context: The product needs behavior to improve discovery, but success-metrics.md explicitly lists raw gallery page views / time-on-gallery as NON-metrics (vanity, could reward passive browsing/copying), and originality rules R2/R7 plus ADR-0001/0002/0003 constrain what behavior may amplify and how it must explain itself. Section 01 requires binding decisions before flywheel code is written.
Decision: (1) Capture behavior as privacy-minimized append-only events (hashed subject keys, Zod-validated primitive payloads, idempotency keys, no content/URLs/prompts â€” ADR-0001/0002/0003 safe-projection boundary). (2) Action-weighted utility: IMPRESSION=1, OPEN=2, SAVE=3, COLLECTION_ADD=4, MCP_RETRIEVAL_USE=5, DISMISSAL=-3, REFORMULATION=5, MODERATOR_ACCEPTANCE=6 (domain constants; impressions de-emphasized per the non-metric rule). (3) Saturation penalties: exponential time decay (exp(-Î»Â·age)) plus a per-item utility cap â€” no permanent popularity lock-in. (4) Diversity rules: max per-creator items in the top window (default 2 in top 20); only ACCEPTED items rank (moderation always overrides engagement); suggestion strength respects the R2 floor (â‰¥3 items AND â‰¥2 creators, ADR-0003 D8). (5) Experiments are minimal deterministic A/B: variant = SHA-256(experimentId:subjectKey) modulo cumulative weight, idempotent assignment, guardrail evaluation returns a PAUSED recommendation (no auto-disable). (6) Explanation signals are provenance-anchored reason codes (QUALITY, RECENT, DIVERSITY, SAVED_SIMILARITY, PATTERN_FREQUENCY, EXPERIMENT_ARM, MODERATOR_APPROVED) per R7.
Consequences: Discovery learns from action and decays popularity; no item or creator can dominate indefinitely; engagement can never override moderation or weaken R2; the flywheel is metadata-only (no content crosses any boundary); experiment assignment is deterministic and auditable; a future feed UI / MCP tool can render reason codes verbatim. This feature ships foundation contracts only (domain + persistence + schema + verifier + telemetry) â€” no UI, no src/mcp, no API routes. Deferred: Î»/cap/weight calibration (via experiments), behavior-event retention, feed/MCP surface integration, cross-device subject identity.
Related: `docs/adr/0004-data-flywheel-and-ranking-feedback.md`, `docs/product/success-metrics.md` (non-metric rule), `docs/product/originality-rules.md` (R2, R7), `docs/adr/0003-provenance-and-originality-policy.md` (D8 floor, D9 safe projection, Â§12 telemetry)

## D-7 - Design system and tokens: token-first visual language with warm neutrals and a single cobalt accent

Date: 2026
Status: Accepted
Context: Section 02 (Experience design) requires one shared visual language across all public and authenticated interfaces. The stack is Tailwind v4 CSS-first (no tailwind.config.*) with shadcn/ui base-nova primitives driven by CSS variables (AGENTS.md AÂ§5). Before this decision, colors and motion had no canonical definition: only default scaffold tokens existed, UI components could drift into hardcoded hex, and dark mode / reduced-motion had no agreed mechanism.
Decision: (1) Adopt a token-first design system: primitive ramps (paper warm neutrals, ink warm dark neutrals, cobalt as the single accent family) plus a shadcn-compatible semantic layer (background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, input, ring, status success/warning/info), all in oklch(), all defined in both light and dark modes, mapped into Tailwind utilities via @theme inline. (2) Theming: light default, .dark class variant, prefers-color-scheme default, manual toggle persisted to localStorage with a FOUC-safe inline head script and a ThemeProvider (no third-party dependency). (3) Editorial technical-index direction: warm neutral foundations, cobalt accent, semantic states, display/sans/mono typography roles (Geist/Geist Mono via next/font), 12/6/4 responsive grid (4 below 768px, 6 at md, 12 at lg), restrained radius, purposeful tokenized motion gated by prefers-reduced-motion. (4) Architecture and constraints are recorded in ADR-0005; the canonical token reference is docs/design/design-tokens.md. This supersedes the D-3 skill-availability note for UI work: taste-skill gates are now available and required before any user-facing UI edit.
Consequences: Future UI features consume tokens, never hardcoded color literals; hardcoded hex in components is a review failure. The accessibility guardrail in success-metrics.md is served by a token contract test asserting WCAG AA (4.5:1) for every semantic pair in both modes. The single-accent lock keeps the product visually consistent; new status hues are added as semantic status tokens, never as accent families. Design decisions are centralized in docs/design/design-tokens.md and ADR-0005. Reduced-motion behavior is enforced at the token layer. No schema or database impact.
Related: ADR-0005, docs/design/design-tokens.md, AGENTS.md AÂ§3 (skill gates) and AÂ§5 (toolchain), docs/product/success-metrics.md (accessibility guardrail)

## D-8 - Portfolio card media metadata: curated HTTPS screenshot reference and bounded stack tags

Date: 2026
Status: Accepted
Context: Section 02 (Experience design) requires a portfolio card system presenting screenshots and high-value metadata consistently across dense galleries, with stable media ratios, responsive images, stack metadata, freshness, source availability, bookmarks, preview, touch parity, keyboard interaction, and no layout shift. Existing GalleryItem/GalleryItemSummary contracts and GalleryCard carry no screenshot/media or stack metadata.
Decision: (1) Add additive nullable GalleryItem.mediaUrl String? as a manually curated external HTTPS screenshot reference - validated HTTPS-only, rejecting javascript:/data:/file: schemes, localhost, loopback, and private-network hosts, capped at 2048 characters; no upload, capture, proxy, CDN, or hosting system; legacy rows stay null (no guessed backfill, ADR-0003 D2). (2) Add default-empty bounded GalleryItem.stackTags String[] for tools/technologies, semantically distinct from styleTags, each trimmed at most 64 characters, at most 10 tags. (3) Reversible additive migration dd_gallery_item_media_url_and_stack_tags (ALTER TABLE ADD COLUMN only); rollback drops only those two columns; attribution, consent, audit, provenance, and content rows untouched. (4) Bookmarks are local-only browser persistence (no backend model/API/auth/sync); preview reveals the same curated media on hover/focus plus an explicit touch/keyboard control and never loads or probes the attributed source page; source availability is the static presence of the attribution link. (5) Responsive native img for arbitrary curated HTTPS hosts; next.config.ts remote image allowlists are not broadened (a controlled CDN enabling Next Image srcset is a future decision). Telemetry stays privacy-minimized: no media URL, source URL, creator PII, or bookmark contents in payloads; public projections never gain contentBlob/structureJSON/raw provenance fields.
Consequences: The shared card system can present stable 16:9 media, stack metadata, freshness from reviewedAt, static source state, local bookmarks, and accessible preview across homepage and /browse while preserving attribution, safe projections, and anti-cloning boundaries. Constraints: no upload/capture/proxy/hosting infrastructure, no detail route/full-content modal/carousel/download, no live source probing, no new accent family or raw color literals, and no new dependencies without documented justification.
Related: docs/adr/0006-portfolio-card-media-metadata.md, docs/adr/0001..0005, docs/product/originality-rules.md (R2, R3), prisma/schema.prisma (GalleryItem), src/domain/curation/schemas.ts (MediaUrlSchema)

## D-9 - Portfolio detail safe reference: attribution-only detail page supersedes the D-8 no-detail-route surface ban

Date: 2026
Status: Accepted
Context: Section 02 (Experience design) requires a Portfolio Detail Page that turns a portfolio record into an informative, attributable reference (desktop/mobile captures, metadata, strengths, page index, sections, stack evidence, source links, creator attribution, capture freshness, similar examples, owner claim, removal/report controls). D-8/ADR-0006 excluded a detail route for the card system. This decision supersedes ONLY that surface-level exclusion for a safe reference page, while preserving every deeper anti-cloning rule (ADR-0001, ADR-0002 D7, ADR-0003 D9, ADR-0004, R1-R8).
Decision: (1) The detail page is an attribution-safe reference surface composed only of safe data: accepted GalleryItemSummary fields, enriched ProvenanceSummary, and bounded curated detail metadata; it never renders contentBlob, structureJSON, raw captures, fingerprints, prompts, hashes, claimant evidence, or reviewer/private data, and has no download/export, carousel, full-content modal, source-page iframe, or live source probing. (2) Reads are guarded to publicly eligible records only: unknown/non-ACCEPTED/FLAG/archived/suspended/rejected/revoked-consent/active-removal records return 404 + noindex; superseding attribution assertions update display without mutating history. (3) Additive bounded curated metadata on GalleryItem: desktopMediaUrl/mobileMediaUrl (curated HTTPS), pageIndex labels, sections descriptors {key,label,present}, strengths reason-code descriptors, stackEvidence {name,evidenceType}; strict PortfolioDetailSchema rejects prohibited fields; no guessed backfill. (4) Similar examples are deterministic accepted/non-FLAG overlap on styleTags+stackTags, capped and attributed, with no ranking/flywheel/PatternSignal dependency. (5) Owner claim and removal/report reuse the existing OwnershipClaim and RemovalRecord workflows; report is surfaced as Request removal, with no new generic Report model. (6) Public claim/removal POSTs require Cloudflare Turnstile server-side verification plus a deployment-backed rate-limit adapter (default 5 per IP per 10 minutes plus daily cap; no process-local-only production fallback). (7) Implementation follows Next 16 App Router conventions (async Promise params, generateMetadata, notFound/noindex, segment loading/error/not-found), existing API route patterns, existing OPEN telemetry only, and existing tokens/primitives with the Taste Skill gate.
Consequences: Builders get an informative, attributable reference for accepted portfolios without a copying surface; owner-claim and removal/report intake are protected and privacy-safe. Constraints: public output strictly limited to safe DTO fields; claim/removal intake requires Turnstile and a real rate-limit binding; no generic report model, ranking-based similar examples, downloads, carousel, or full-content modal. ADR-0006/D-8 remain accepted for the card system; only the detail-route surface decision is superseded. Supersedes: D-8 detail-route clause only. D-1 through D-7 remain in force.
Related: docs/adr/0007-portfolio-detail-safe-reference.md, docs/adr/0006-portfolio-card-media-metadata.md (detail-route clause superseded), docs/adr/0001..0005, docs/product/originality-rules.md (R1-R8), docs/product/provenance-and-originality-policy.md (§7, §8, §11)
